---
"qufox-docs": patch
---

UI polish: card titles, list rows, and whole-card links now inherit the design
system's text color instead of showing as default browser links; the content
column uses the standard (wider) container instead of the narrow one; and the
"On this page" table of contents is a collapsible panel, closed by default on
every viewport.
