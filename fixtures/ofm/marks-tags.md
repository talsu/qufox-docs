A ==highlighted phrase== and a #project tag plus a nested #inbox/to-read tag.

Not a tag: #1984 (digits only). Code stays literal: `#nope` and `==keep==`.

```
#also-not-a-tag inside a fence
```

An unsupported diagram:

```mermaid
graph TD; A-->B;
```
